import { Store } from '@ngrx/store'

export class NotOk {
  constructor(store: Store<PersonsState>){}
                           ~~~~~~~~~~~~                                           [no-typed-store]
}

export class NotOk2 {
  constructor(cdr: ChangeDetectionRef, private store: Store<CustomersState>){}
                                                            ~~~~~~~~~~~~~~        [no-typed-store]
}

export class NotOk3 {
  constructor(private store: Store<any>, private personsService: PersonsService){}
}

export class Ok {
  constructor(store: Store<{}>)
}

export class Ok2 {
  constructor(store: Store<object>)
}

export class Ok3 {
  constructor(store: Store<object>)
}

[no-typed-store]: Store should not be typed, for v8 use `Store<object>` instead, for v9 use `Store`
